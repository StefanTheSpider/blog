<%- include('partials/header') %>
  <h1>home</h1>
  <p>
    <%= mainContent %>
  </p>

  <% blogPosts.forEach(function(post) { %>
    <h2>
      <%= post.title %>
    </h2>
    <p>
      <%= post.content %>
    </p>
    <% }) %>

      <%- include('partials/footer') %>